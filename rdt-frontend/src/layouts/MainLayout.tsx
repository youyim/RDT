import { Outlet } from 'react-router-dom';

/**
 * Main Layout for authenticated application
 * Minimal skeleton - customize sidebar/header as needed
 * @returns Main layout component
 */
export function MainLayout() {
  return <Outlet />;
}
